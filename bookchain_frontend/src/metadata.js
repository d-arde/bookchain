import * as pythonMetadata from "./python_metadata.json";
import * as marketingMetadata from "./marketing_metadata.json";
import * as electronmagneticsMetadata from "./metadata.json";

const python = {
  name: pythonMetadata.name,
};

const marketing = {
  name: marketingMetadata.name,
};

const electromagnetics = {
  name: electronmagneticsMetadata.name,
};

export { python, marketing, electromagnetics };
